# 移除新手教學「評分比重」頁 — 設計文件

- **日期**:2026-07-21
- **狀態**:已與使用者確認,待實作
- **範圍**:`extension/dashboard/`(Welcome Modal)
- **關聯**:[2026-07-21 移除 AI 分析](2026-07-21-remove-ai-analysis-design.md) 把教學第 4 頁從「AI 評分權重分析(API 引導)」改寫為「評分比重(可選)」;本文件把這頁整個移除。

---

## 1. 背景與動機

使用者方向:精簡頁面。設定頁已隨 AI 分析移除,教學第 4 頁(原 API 引導頁,現「評分比重(可選)」)
內容自承「這頁純屬選用,可以直接跳過」,不值得占一頁,直接刪除,教學從 5 步變 4 步。

## 2. 變更內容

**`index.html`**
- 刪除 `wstep-4`(評分比重)整個 step 區塊與第 5 顆導覽圓點
- 原 `wstep-5`(一切就緒)改編號為 `wstep-4`(含子元素 id:`-title`/`-body`/`-done1..3`)
- 眉標全部改為 `x / 4`
- 幾何:`.welcome-track` 寬 500% → 400%;`.welcome-step` 寬 20% → 25%

**`dashboard.js`**
- `welcomeGoStep` 位移係數 20% → 25%
- `_welcomeUpdateButtons`:最終步判斷 `n === 5` → `n === 4`,其上一步按鈕 `data-wgo` 4 → 3
- i18n(三語):刪除 `wTitle4`/`wBody4a-d`(評分比重文案);原 `wTitle5`/`wBody5` 更名為
  `wTitle4`/`wBody4`;`wDone1-3` 不變;`setText` 綁定同步改為 `wstep-4-*`

## 3. 不做

- 不動教學前三步內容
- 不動 `?welcome=1` 首次安裝自動開啟邏輯
- 評分比重功能本身(課程詳情頁圓餅圖 + 手動編輯)不受影響

## 4. 驗證

- harness 實測:4 步、4 圓點、眉標 1/4–4/4、位移 -75%、「開始使用」關閉、上一步導覽正常
- 語言切換(en / zh-CN / zh-TW)後最終步文案綁定正確
- 全部單元測試綠;`node --check` 通過
